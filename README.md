Be sure to download the preprocessing.py and style.py files, as well as all files in the plots folder.
We recommend leaving all HTML files in the plots folder within the folder you named "plots" so that you don’t have to make too many changes.
Open the preprocessing.py file, then run it in your terminal so that you’ll be directed to view the dashboard results.
